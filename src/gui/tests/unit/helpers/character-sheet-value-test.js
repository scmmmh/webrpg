import { characterSheetValue } from 'gui/helpers/character-sheet-value';
import { module, test } from 'qunit';

module('Unit | Helper | character sheet value');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = characterSheetValue([42]);
  assert.ok(result);
});
