import { formatChatMessage } from 'gui/helpers/format-chat-message';
import { module, test } from 'qunit';

module('Unit | Helper | format chat message');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatChatMessage([42]);
  assert.ok(result);
});
