<div class="row">
  <div class="small-12 column">
    <h1>Games in Progress</h1>
  </div>
</div>
<div class="row">
  <div class="small-12 column">
    <div class="text-right">
      {{#link-to 'games.new' class='button small'}}Start a new Game{{/link-to}}
    </div>
    {{outlet}}
    <ul class="no-padding no-margin">
      {{#each}}
        <li class="panel clearfix">
          {{#if joined}}
            {{#link-to 'game' id}}{{title}}{{/link-to}}
          {{else}}
            <div class="right"><a href="#" {{action 'join_game' id}} class="button tiny no-margin">Join</a></div>
            {{title}}
          {{/if}}
        </li>
      {{/each}}
    </ul>
  </div>
</div>
